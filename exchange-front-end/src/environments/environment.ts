// Este arquivo pode ser substituído durante a construção usando a matriz `fileReplacements`.
// `ng build --prod` substitui `environment.ts` por `environment.prod.ts`.
// A lista de substituições de arquivos pode ser encontrada em `angular.json`.

export const environment = {
  production: false
};

/*
 * Para facilitar a depuração no modo de desenvolvimento, você pode importar o seguinte arquivo
 * para ignorar as trilhas de erro relacionadas à zona, como `zone.run`, `zoneDelegate.invokeTask`.
 *
 * Esta importação deve ser comentada no modo de produção, pois terá um impacto negativo
 * no desempenho se um erro for lançado.
 */
// import 'zone.js/dist/zone-error';  // Incluído com o Angular CLI.
